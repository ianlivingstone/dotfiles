# SSH Configuration
# Machine-specific configuration is included from XDG config directory

# Include machine-specific config (must be first for proper precedence)
Include ~/.config/ssh/machine.config

# Global SSH settings
Host *
    # Keep connections alive
    ServerAliveInterval 60
    ServerAliveCountMax 30
    
    # Connection settings
    Compression yes
    
    # Connection multiplexing for faster subsequent connections
    ControlMaster auto
    ControlPath ~/.ssh/sockets/%r@%h-%p
    ControlPersist 10m
    
    # Create socket directory if it doesn't exist
    # (This is handled by the shell, but documented here)