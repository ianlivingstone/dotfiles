# Dotfiles package configuration
# Format: package[:target]
# If no target specified, defaults to $HOME
# Variables like $XDG_CONFIG_DIR will be expanded

# Home directory configs
zsh
git
ssh
tmux
misc

# XDG config directory (.config) - Global configs for all projects
nvim:$XDG_CONFIG_DIR/nvim
claude-code:$XDG_CONFIG_DIR/claude-code

# Note: gnupg config is generated by dotfiles.sh (GPG doesn't support includes)
# Note: .claude/ directory contains project-specific settings for THIS repo only